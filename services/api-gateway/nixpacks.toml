[phases.setup]
nixPkgs = ["jdk21", "gradle"]

[phases.build]
cmds = [
  "chmod +x ../../gradlew || true",
  "cd ../.. && ./gradlew :services:api-gateway:build -x test"
]

[phases.start]
cmd = "cd ../.. && java -jar services/api-gateway/build/libs/api-gateway.jar"

