{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "chmod +x ../../gradlew && cd ../.. && ./gradlew :services:user-service:build -x test"
  },
  "deploy": {
    "startCommand": "java -jar ../../services/user-service/build/libs/user-service.jar",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

